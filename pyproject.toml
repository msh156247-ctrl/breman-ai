# Vercel이 이 저장소를 Python으로 빌드할 때 진입점을 알 수 있게 합니다.
# ⚠️ 실제 운영은 Root Directory = `frontend` 로 Next만 배포하고,
#    FastAPI는 Render/Railway/VM 등에서 `python -m api.server` 로 띄우는 것을 권장합니다.
#    (서버리스 WebSocket·SQLite·실행 시간 제한 이슈)

[tool.vercel]
entrypoint = "api/server.py"
